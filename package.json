{
    "name": "fitness-tracker",
    "version": "1.0.0",
    "description": "FitnessTracker - Full Stack MERN Application",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "vercel-build": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "express-validator": "^7.0.1",
        "jsonwebtoken": "^9.0.2",
        "mongoose": "^8.1.1"
    },
    "engines": {
        "node": "18.x"
    }
}